name: discover_yourself_meditation
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  vector_math: ^2.1.4
  webview_flutter: ^4.9.0
  flutter_animate: ^4.1.1+1
  shared_preferences: ^2.2.2
  confetti: ^0.7.0
  liquid_swipe: ^3.1.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
